# This file is auto-generated by the configure script.

{%- for entry in cache_entries %}
if(NOT DEFINED {{ entry.name }})
  set({{ entry.name }} "{{ entry.value }}" CACHE {{ entry.type }} "Configuration variable")
endif()
{% endfor %}
